import Node.comp.Bar;
import Edge.comp.O;

class Foo extends Bar{
    int a;
    int[] b;
    String s;
    boolean d;

    public static void main(String[] args) {}

    public int red(int a, bool b){
        return a;
    }

    public int foo(int anInt, int[] anArray, boolean aBool, String aString) {
        int mati;
        O bibs;
        int dre;
        //this.red(mati, aBool);
        O.boas();
        mati = anArray[1*23*4*1*3] * a;
        !(mati + a < mati);
        bibs = new O();
        bibs.go(a,3);
        return a;
    }
}